const Spinner = () => {
  return (
    <div
      role="status"
      className="w-4 h-4 border-4 border-t-transparent border-gray-500 rounded-full animate-spin"
    />
  );
};

export default Spinner;
